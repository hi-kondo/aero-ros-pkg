<launch>

  <arg name="map_file" default="$(find aero_startup)/aero_move_base/maps/map2.yaml"/>

  <!-- Run the map server -->
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" >
    <remap from="map" to="map_2"/>
  </node>

</launch>
